import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Join Us | =PRC= Clan',
  description: 'How to join the =PRC= clan',
}

export default function JoinUsLayout({ children }) {
  return children
}
